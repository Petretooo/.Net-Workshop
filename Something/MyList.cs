using System;
using System.Collections.Generic;
using System.Text;

namespace Something
{
    class MyList
    {


        private const int initialNumber = 10;
        private int numberofElements = initialNumber;
        private int[] elements;

        











































        /*
        int[] arr;

        public MyList()
        {

        }




        public void add(int element)
        {

            int e = element;
            for (int i = 0; i < this.arr.Length; i++)
            {
                if (arr.Length - 1 != null)
                {
                    int j = arr.Length;
                    this.arr = new int[j++];
                }

                if (this.arr[i] != 0)
                {
                    continue;
                }else if(this.arr[i] == 0)
                {
                    this.arr[i] = e;
                    break;
                }

            }
        }




        public void AtIndex(int index)
        {
            int indexOf = index;
            if(arr[indexOf] != 0)
            {
                Console.WriteLine(arr[indexOf]);
            }
            else
            {
                Console.WriteLine("Nothing");
            }
        }



        public void removeAt(int index)
        {
            int atInd = index;
            if(arr[atInd] != 0)
            {
                arr[atInd] = 0;
            }
        }



        public void removeAll()
        {
            for (int i = 0; i < arr.Length; i++)
            {
                if(this.arr[i] != 0)
                {
                    this.arr[i] = 0;
                }
                else
                {
                    continue;
                }
            }
        }


        public void printAll()
        {
            for (int i = 0; i < this.arr.Length; i++)
            {
                if(this.arr[i] != 0)
                {
                    Console.WriteLine(this.arr[i]);
                }
            }
        }
        */
    }
}
